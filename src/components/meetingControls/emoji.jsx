import React from 'react';

const Emoji = () => {
  const onClick = () => {
    // call a dispatch() here;
  };

  return (
    <button type="button" onClick={onClick}>
      Emoji will go here!
    </button>
  );
};

export default Emoji;
